import { Montserrat } from "./montserrat";
import { OpenSans } from "./openSans";

export const pageTitle      = `${Montserrat.className} text-page-title`
export const sectionTitle   = `${Montserrat.className} text-section-title`
export const subTitle       = `${Montserrat.className} text-sub-title`
export const cardTitle      = `${Montserrat.className} text-card-title`
export const navigation     = `${Montserrat.className} text-navigation`
export const body           = `${OpenSans.className} text-body`

export {
    Montserrat,
    OpenSans
}